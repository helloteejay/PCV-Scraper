# Python
__pycache__/
*.pyc
.venv/
venv/

# Local env / secrets — never commit these
.env

# Debug artifacts are uploaded by CI as run artifacts, not committed
debug/

# Rendered copies of the README (generated on demand, not source)
README.html
README.pdf

# Run state is persisted via the GitHub Actions cache, not committed
state/seen.json
